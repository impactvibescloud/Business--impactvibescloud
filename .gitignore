# See https://help.github.com/ignore-files/ for more about ignoring files.

# dependencies
/node_modules
package-lock.json
yarn.lock

# testing
/coverage

# production
# /build (removed to allow build folder tracking)

# misc
.eslintcache
.DS_Store
.idea
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# NOTE: dist is intentionally NOT ignored
# (remove below line if it existed)
/dist  # <-- This line is removed to ensure dist is tracked
